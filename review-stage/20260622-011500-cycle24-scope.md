# cycle 24 Scope

- Reviewer: Copilot Claude Opus paper-first quant PM review
- Review file: review-stage/20260622-011500-cycle24-quant-pm-copilot-opus.md
- Executor: Codex cloud
- Log dir: /root/research_project_aris/output/aris_paper_pm_20260621-193610
- Started: 2026-06-22T01:24:17+00:00

## Pending
Completed by Codex executor.

## Implemented

- Revised `report/sp500_case_study.tex` front matter to lead with the actual
  cross-regime signal-existence evidence for 1-day momentum rather than using
  the post-2022 improvement statistic as the primary headline.
- Tightened the Route B horizon framing: the 30-day residual overlay is now
  explicitly a separate risk-sleeve diagnostic, not an improvement claim on the
  endorsed 5-day benchmark or an assembled book.
- Marked diagnostic Route B rows more clearly as unselected/unaudited and
  rewrote the DSR audit rows so the paper leads with failure under both
  same-target and full-grid trial counts.
- Strengthened capacity language: $500M/$1B stress rows are now called
  infeasible extrapolations beyond the 10%-ADV ceiling, not capacity clearance.
- Added the horizon-matched 5d post-training PIT sensitivity as a limitation
  (Sharpe 0.645 current-membership, 0.518 PIT-filtered, CI crossing zero) rather
  than empirical alpha evidence.
- Rebuilt `report/sp500_case_study.pdf`.

## Focused computation decision

The reviewer's highest-ROI experiment was a paired 5d Route B overlay plus DSR
audit. The report artifacts include a 5d PIT post-training sensitivity and the
README names a 5d selected run directory, but the visible checkout does not
contain the required `output/model_search/.../predictions_val_test.parquet`
files. Without those saved predictions, the paired-increment/DSR overlay audit
would require retraining or recovering remote model-search outputs, which is too
large and not a focused paper fix for this cycle. The report now documents the
5d overlay audit as required next evidence instead of treating the 30d overlay
as horizon-matched support.

## Validation

- `git diff --check` passed.
- `pdflatex -interaction=nonstopmode -halt-on-error sp500_case_study.tex` run
  twice from `report/`; output remains 10 pages.
- No Python scripts were touched, so Python compile was not applicable.
- Local commit created with message `Refine cycle 24 paper framing`.
- Push attempted and failed because HTTPS credentials were unavailable in the
  non-interactive runtime: `fatal: could not read Username for 'https://github.com':
  terminal prompts disabled`.

## Remaining risks

- The paired 5d Route B overlay/DSR audit remains missing until saved 5d
  predictions are available or the residual model search is intentionally rerun.
- The paper remains gross-of-borrow in the main evaluator and only partially
  point-in-time; those limitations are disclosed in the report.
