# cycle 19 Scope

- Reviewer: Copilot Claude Opus paper-first quant PM review
- Review file: review-stage/20260622-000847-cycle19-quant-pm-copilot-opus.md
- Executor: Codex cloud
- Log dir: /root/research_project_aris/output/aris_paper_pm_20260621-193610
- Started: 2026-06-22T00:14:10+00:00

## Pending
Completed by Codex executor.

## Fix Scope

- Tightened `report/sp500_case_study.tex` around the cycle 19 Opus P1 horizon
  finding: Route B is now explicitly framed as a 30-day horizon-specific
  drawdown/risk sleeve, not evidence that ML improves the endorsed 5-day
  momentum benchmark.
- Added a focused return-stream negative control for Route B:
  `scripts/build_route_b_vol_timing_control.py` and
  `report/artifacts/route_b_vol_timing_control/`.
- Updated the report to state that Route B fails a 0.95 DSR threshold under
  both same-target and full-grid trial counts.
- Sharpened capacity language: the square-root impact stress holds turnover
  fixed, and the reported 25.3% p95 participation at $1B implies an approximate
  10% ADV ceiling near $395M under linear AUM scaling.
- Condensed repeated cautionary prose to keep the compiled report at 10 pages;
  Kronos remains future PIT-universe infrastructure only, not alpha evidence.

## Focused Computation

Ran:

```bash
python3 scripts/build_route_b_vol_timing_control.py
```

Result: the shifted six-rebalance inverse-volatility control on 30-day momentum
returns did not reproduce Route B's drawdown profile. On the aligned 32-period
comparison, vol-timed momentum Sharpe was 0.732 with max drawdown -14.0%, versus
Route B Sharpe 0.924 with max drawdown -7.3%. This is a coarse return-stream
negative control, not an executable single-name capacity test.

## Validation

- `git diff --check`: pass.
- `python3 -m py_compile scripts/build_route_b_vol_timing_control.py`: pass.
- `pdflatex` two-pass compile: pass.
- PDF page count: LaTeX log reports `report/sp500_case_study.pdf` at 10 pages.
  `pdfinfo` is not installed on this server, so page count was read from the
  LaTeX build log.

## Reviewer Gate

Used the supplied read-only Copilot Opus review:
`review-stage/20260622-000847-cycle19-quant-pm-copilot-opus.md`.
No fresh MCP reviewer rerun was available in this Codex runtime.

## Commit / Push

- Local commit created with message `Tighten Route B paper framing`.
- Push attempted with `git push`, but failed because HTTPS credentials were not
  available in the non-interactive runtime:
  `fatal: could not read Username for 'https://github.com': terminal prompts disabled`.
  Push remains pending.
