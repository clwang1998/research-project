# round17 Modification Scope

## Review Handoff

- Reviewer: GitHub Copilot Opus
- Executor: Codex
- Prompt file: `tmp/copilot_opus_review_prompt.md`
- Review response file: `review-stage/20260621-1521-round17-copilot-opus.md`
- Scope: `scripts/train_supervised_gat.py, scripts/run_validation_ensemble_search.py, scripts/run_overnight_supervised_gat_search.sh`

## Codex Changes In Response

- Added explicit supervised GAT relation ablations: all, single-relation, pairwise, shuffled-edge random placebo, and no-edge baseline.
- Made downstream supervised graph variants select target- and ablation-matched embedding runs instead of the latest target-matched run.
- Reduced default MLP search space to smaller networks and added weight-decay variation.
- Added `ensemble_mlp_gated` and `ensemble_graph_ablation_tree` outputs alongside existing all/no-MLP/tree ensembles.
- Extended the overnight driver to train GAT ablations, run tree graph-ablation search separately, and keep MLP search on the main feature variants.

## Validation

- `python -m py_compile scripts/train_supervised_gat.py scripts/run_validation_ensemble_search.py`
- `bash -n scripts/run_overnight_supervised_gat_search.sh`
- Local smoke GAT: no-edge and sector ablations generated schema-compatible `supervised_gat_oof_embeddings.parquet`.
- Local smoke downstream search: ridge ran on `supervised_graph_sector` and `supervised_graph_no_edges` variants with distinct feature_variant labels.
- Remote syntax check passed on `/root/research_project_supervised_gat`.
- Remote full run launched as `overnight_graph_ablation_mlp_small`; by the first monitoring pass, all/sector/style_knn/rolling_corr GAT ablations were completing normally and CPU prefill had begun writing completed candidates.

## Remaining Risk

- `mcp__manual_review__review` was not exposed in this runtime; `tmp/copilot_opus_review_prompt.md` was generated and copied for manual VS Code Copilot Opus review.
- Full remote experiment is still running; final model/ensemble conclusions must wait for `overnight_graph_ablation_mlp_small_*metrics.csv`.
