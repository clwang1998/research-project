# round14 Modification Scope

## Review Handoff

- Reviewer: GitHub Copilot Opus
- Executor: Codex
- Prompt file: `tmp/copilot_opus_review_prompt.md`
- Review response file: `review-stage/20260621-1335-round14-copilot-opus.md`
- Scope: `scripts/run_validation_ensemble_search.py`

## Codex Changes In Response

- Added validation-selected ensemble variants in `scripts/run_validation_ensemble_search.py`:
  `ensemble_all_val_selected`, `ensemble_no_mlp`, `ensemble_tree_only`,
  `ensemble_supervised_graph_tree`, and `ensemble_stability_filtered`.
- Added ensemble-level regime metrics output and report sections for comparing
  test, available regime slices, and costed backtest metrics.
- Fixed the Copilot Opus M1-M3 follow-up findings:
  downstream supervised-graph runs now require an explicit target-matched
  embedding path, `train_supervised_gat.py` still maintains `out_dir/latest` as
  a convenience pointer, supervised graph features are joined only on
  `date,symbol` with a duplicate-key guard and missing-share logging, and the
  overnight driver validates target parquet schema before reusing cached data.
- Copilot Opus MCP tool `mcp__manual_review__review` was not exposed in this
  runtime. The handoff prompt was generated at `tmp/copilot_opus_review_prompt.md`
  and copied to the clipboard for manual VS Code Copilot Chat review.
- The manual VS Code Copilot Chat response was recovered from VS Code chat
  session storage and saved to
  `review-stage/20260621-1335-round14-copilot-opus.md`.

## Validation

- `python -m py_compile scripts/run_validation_ensemble_search.py`
- `python -m py_compile scripts/run_model_pipeline.py scripts/train_supervised_gat.py scripts/run_validation_ensemble_search.py scripts/run_walk_forward.py`
- `bash -n scripts/run_overnight_supervised_gat_search.sh`
- Local smoke test for `update_latest_outputs`.
- Local schema check detected stale cached targets missing 10d columns, which
  exercises the new rebuild trigger.
- Remote syntax checks passed after syncing the updated scripts.
- Remote supervised-graph join diagnostic found `key_only_not_meta=0` for all
  eight existing supervised GAT embedding runs, so previous overnight metrics
  were not affected by the wider metadata join key.
- Remote skip-run aggregation completed successfully on
  `/root/research_project_supervised_gat`, writing refreshed ensemble metrics,
  regime metrics, and report under `output/model_search/`.

## Remaining Risk

- Copilot Opus reported no P0/P1 blocker, no direct leakage blocker, and no
  unsafe shell blocker. It flagged M1-M3 as medium reproducibility/correctness
  risks; these have now been fixed locally.
- Current train/val/test split only evaluates `covid_2020` in validation and
  `inflation_2022` in test; `gfc_2008` requires a separate walk-forward split
  that does not train on 2008 labels.
