# round15 Modification Scope

## Review Handoff

- Reviewer: GitHub Copilot Opus
- Executor: Codex
- Prompt file: `tmp/copilot_opus_review_prompt.md`
- Review response file: `review-stage/20260621-1434-round15-copilot-opus.md`
- Scope: `scripts/run_model_pipeline.py, scripts/train_supervised_gat.py, scripts/run_walk_forward.py, scripts/run_overnight_supervised_gat_search.sh, review-stage/20260621-1335-round14-copilot-opus.md, review-stage/20260621-1335-round14-scope.md`

## Codex Changes In Response

- Addressed round14 Copilot Opus M1-M3 findings:
  - M1: Removed implicit `latest/` default from downstream supervised-graph
    consumers; `--supervised-graph-embedding-path` is now explicitly required
    when supervised graph features are included. `train_supervised_gat.py` still
    writes `out_dir/latest` as an explicit convenience pointer.
  - M2: Changed supervised graph feature joins to use `date,symbol` only,
    matching the fixed graph join path, with duplicate-key guard and missing
    feature-share logging.
  - M3: Added target parquet schema validation to the overnight driver before
    reusing cached processed data.
- Saved the recovered round14 Copilot Opus response under `review-stage/`.

## Validation

- `python -m py_compile scripts/run_model_pipeline.py scripts/train_supervised_gat.py scripts/run_validation_ensemble_search.py scripts/run_walk_forward.py`
- `bash -n scripts/run_overnight_supervised_gat_search.sh`
- Local smoke test for `train_supervised_gat.update_latest_outputs`.
- Local target schema check detected stale cached 10d target columns as
  expected.
- Synced updated scripts to `/root/research_project_supervised_gat`.
- Remote syntax checks passed with `.venv/bin/python -m py_compile ...` and
  `bash -n`.
- Remote parser smoke confirmed supervised graph embedding path defaults are
  now `None`.
- Remote supervised-graph join diagnostic found `key_only_not_meta=0` for all
  eight existing supervised GAT embedding runs.

## Remaining Risk

- `mcp__manual_review__review` is still not exposed in this runtime. The round15
  Copilot Opus prompt was generated and copied to the clipboard, but a manual
  VS Code Copilot Chat response has not been pasted back yet.
- Existing overnight metrics did not need recomputation for M2 because the
  remote join diagnostic found no metadata-key mismatches in the existing
  supervised embedding panels.
