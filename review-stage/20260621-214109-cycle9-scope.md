# cycle 9 Scope

- Reviewer: Copilot Claude Opus paper-first quant PM review
- Review file: review-stage/20260621-214109-cycle9-quant-pm-copilot-opus.md
- Executor: Codex cloud
- Log dir: /root/research_project_aris/output/aris_paper_pm_20260621-193610
- Started: 2026-06-21T21:47:43+00:00

## Pending
Replaced by this completed scope note.

## Changes

- Added `scripts/build_pit_inclusion_sensitivity.py`, a focused post-training
  audit builder for `sp500_member_asof == 1` point-in-time inclusion
  sensitivity.
- Generated `report/artifacts/pit_inclusion_sensitivity/` with summary,
  backtest streams, README, and run metadata.
- Updated `report/sp500_case_study.tex` and rebuilt `report/sp500_case_study.pdf`
  to:
  - lead the headline with the PM decision: keep low-turnover 12--1 momentum as
    benchmark/core and do not promote ML/graph/residual models as standalone
    alpha;
  - report the new PIT inclusion sensitivity: 30d momentum Sharpe
    `0.741 -> 0.446`; Route B post-training overlay Sharpe `0.987 -> 0.725`;
  - distinguish the partial PIT inclusion check from the parametric
    delisting-haircut sensitivity;
  - add the annualization-convention, DSR pooling, and 2008 warm-up caveats.

## Key Computation

`python3 scripts/build_pit_inclusion_sensitivity.py`

- Current members added on/after 2022-01-01: 72.
- Excluded hold-out symbols after baseline controls: 61.
- 30d momentum current-membership: Sharpe `0.740824`, annual return `7.789968%`,
  max drawdown `-13.8252%`, turnover `0.401163`.
- 30d momentum PIT inclusion: Sharpe `0.446085`, annual return `3.910285%`,
  max drawdown `-17.4559%`, turnover `0.417093`.
- Route B overlay current-membership post-training: Sharpe `0.986523`, annual
  return `8.274841%`, max drawdown `-7.3242%`, turnover `0.494109`.
- Route B overlay PIT inclusion post-training: Sharpe `0.725269`, annual return
  `5.687635%`, max drawdown `-10.0448%`, turnover `0.513361`.

## Validation

- `python3 -m py_compile scripts/build_pit_inclusion_sensitivity.py scripts/eval_momentum_baseline.py scripts/run_alpha_robustness_audit.py scripts/run_model_pipeline.py`
- `python3 scripts/build_pit_inclusion_sensitivity.py`
- `pdflatex -interaction=nonstopmode -halt-on-error sp500_case_study.tex` twice
  from `report/`; output remains 10 pages. The LaTeX log has one non-blocking
  underfull hbox in the robustness table after converting it to wrapping
  columns.
- `git diff --check`

`pdfinfo` and `pdftotext` are not installed in this runtime, so page count and
PDF smoke checks rely on the `pdflatex` log.

## Commit / Push

- Local commit created with subject
  `Add PIT inclusion sensitivity to ARIS paper cycle 9`.
- Push attempted with `git push`, but failed because GitHub HTTPS credentials
  were unavailable in the noninteractive terminal:
  `fatal: could not read Username for 'https://github.com': terminal prompts disabled`.
  Push is pending.

## Reviewer Gate

- Input review: `review-stage/20260621-214109-cycle9-quant-pm-copilot-opus.md`
  was read first and treated as the read-only Copilot Opus quant-PM review.
- Post-fix Copilot Opus rerun was not performed because
  `mcp__manual_review__review` is not exposed in this Codex runtime and there is
  no interactive VS Code Copilot handoff available while unattended.

## Remaining Risk

- The PIT overlay row is a post-training sensitivity, not a retrained PIT model
  search.
- Delisted/deleted constituents and delisting returns are still absent; the
  paper now states that neither PIT inclusion nor haircut sensitivity is a true
  survivorship correction.
