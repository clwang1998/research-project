# round29 Modification Scope

## Review Handoff

- Reviewer: GitHub Copilot Opus, current VS Code Copilot Chat.
- Executor: Codex.
- Review response file: `review-stage/20260622-0220-round29-copilot-opus.md`.
- Scope:
  - `docs/route_b_residual_alpha_20260622.md`
  - `report/sp500_case_study.tex`
  - `report/sp500_case_study.pdf`
  - synchronized ignored artifacts under `output/model_search/route_b_factor_residual_alpha_core_20260622*`
  - synchronized ignored artifacts under `output/alpha_robustness_audit_route_b_30d/`

## Codex Changes In Response

- Mirrored Route B model-search outputs, 30d ultra-blend metrics, predictions,
  ensemble files, logs, and P0 robustness-audit CSVs from the GPU server into
  the local ignored `output/` tree.
- Verified that Route B table values can now be traced locally from
  `output/model_search/route_b_factor_residual_alpha_core_20260622_30d_ultra_blend_metrics.csv`
  and that DSR/PBO values can be traced from
  `output/alpha_robustness_audit_route_b_30d/`.
- Updated `docs/route_b_residual_alpha_20260622.md` to state that artifacts are
  mirrored locally, removed lambda=0.3 from the main result table, and reframed
  the conclusion as a candidate risk-timing sleeve rather than confirmed alpha.
- Updated `report/sp500_case_study.tex` to replace the remote-only artifact
  caveat with a local artifact/provenance note and a portfolio-convention
  sensitivity note.
- Removed the lambda=0.3 row from the main paper table.
- Compressed secondary diagnostic tables/figures into text summaries so the
  report remains within the 10-page limit.

## Validation

- Synchronized local artifacts:
  - 37 Route B model-search paths under `output/model_search/`
  - P0 audit CSV/JSON/Markdown files under `output/alpha_robustness_audit_route_b_30d/`
  - Route B nohup log under `output/route_b/`
- Extracted local Route B metrics from the synchronized CSVs:
  - `z_blend`, lambda 0.2, xgb_balanced test RankIC 0.028341, ICIR 0.768238,
    Sharpe 0.969540, annual return 8.0804%, max drawdown -6.9599%, turnover
    0.491806.
  - full-grid DSR 0.003006, same-target DSR 0.813601, PBO 0.5000.
- `python -m py_compile` passed for `scripts/run_model_pipeline.py`,
  `scripts/run_validation_ensemble_search.py`,
  `scripts/build_signal_horizon_ic_surface.py`, and
  `scripts/build_factor_sleeve_book.py`.
- `git diff --check` passed for the modified report/doc files.
- `pdflatex` two-pass compile passed; `report/sp500_case_study.pdf` is 10 pages.

## Remaining Risk

- `output/` is gitignored, so the synchronized Route B artifacts are present
  locally but will not be committed unless the project intentionally tracks a
  compressed/reduced artifact bundle elsewhere.
- The Route B result remains exploratory: full-grid DSR fails, CSCV PBO is 50%,
  and the Rank IC improvement is negligible.
- The report still notes a portfolio-convention sensitivity between the Route B
  pipeline baseline and `eval_momentum_baseline.py`.
