# cycle 4 Scope

- Reviewer: Copilot Claude Opus paper-first quant PM review
- Review file: review-stage/20260621-202613-cycle4-quant-pm-copilot-opus.md
- Executor: Codex cloud
- Log dir: /root/research_project_aris/output/aris_paper_pm_20260621-193610
- Started: 2026-06-21T20:33:02+00:00

## Pending
Replaced by this completed scope note.

## Fixes Applied

- Reframed the report's first-page headline and conclusion so the primary claim
  is the negative/deflated result: no broad multivariate, graph, MLP, or
  residual-overlay construction establishes statistically deflated alpha beyond
  12--1 momentum on the current-membership S&P 500 dataset.
- Demoted the Route B 30-day overlay wording to a point-estimate research lead:
  Sharpe 0.740->0.987 and drawdown -13.8%->-7.3% are now stated next to
  N=35, DSR 0.003, PBO 50%, and Fama--MacBeth t=0.05.
- Removed winner-style bolding from unaudited 30-day LightGBM rows and labeled
  the row as unaudited by DSR/PBO; the 30-day block is now explicitly too thin
  to rank plain-model and overlay Sharpes.
- Added a horizon-selection caveat: Route B moved to 30d because shorter
  horizons did not deliver reliable net economics, so the overlay must pay the
  full multiple-testing charge.
- Clarified that capacity, survivorship haircut, and factor-neutral robustness
  rows are sensitivity transforms of the same selected N=35 overlay stream, not
  independent confirmations.
- Relabeled the factor-neutral audit row so the Sharpe cannot be read as
  independent alpha: return falls 8.27%->5.17%, turnover rises 0.49->0.70, and
  FM t=0.05.
- Regenerated the frequency-surface and factor-sleeve diagnostics from the cloud
  feature artifacts and copied lightweight CSV snapshots into tracked
  `report/artifacts/` paths because `output/` is a cloud symlink.

## Focused Computation

- `python3 scripts/build_signal_horizon_ic_surface.py`
  - Wrote CSVs under `output/signal_horizon_surface/` and copied them to
    `report/artifacts/signal_horizon_surface/`.
  - Matplotlib is not installed on the server, so the optional heatmap rewrite
    was skipped; the existing report figure was left unchanged.
- `python3 scripts/build_factor_sleeve_book.py`
  - Wrote CSVs under `output/factor_sleeve_book/` and copied them to
    `report/artifacts/factor_sleeve_book/`.
  - Matplotlib is not installed on the server, so the optional equity-curve
    rewrite was skipped; the existing report figure was left unchanged.
  - Updated report prose to the regenerated CSV values: momentum-only Sharpe
    0.598, reversal-only Sharpe -0.519, combined inverse-vol Sharpe 0.094,
    DSR 0.055, bootstrap CI [-0.724, 1.095], weights 43.1%/56.9%.

## Remaining Limitations

- Plain 30-day LightGBM/XGBoost/Ridge per-period artifacts are still not
  archived in the Route B audit format, so they were not rerun through DSR/PBO
  in this cycle.
- The 30-day Route B comparison still rests on only 35 non-overlapping hold-out
  periods and should remain exploratory.
- Survivorship is still bounded by stress haircuts rather than corrected with a
  point-in-time constituent universe and delisting returns.
- The optional PNG regeneration for frequency and sleeve figures was skipped
  because matplotlib is missing in this runtime; CSV traceability is now present.

## Validation

- `git diff --check` passed.
- `python3 -m py_compile scripts/build_signal_horizon_ic_surface.py scripts/build_factor_sleeve_book.py scripts/prepare_copilot_opus_review.py` passed.
- `pdflatex -interaction=nonstopmode -halt-on-error sp500_case_study.tex` passed twice from `report/`; output PDF remains 9 pages.
- LaTeX log scan found no unresolved-reference, overfull, or underfull warnings.

## Reviewer Gate

- Used the provided Copilot Opus cycle-4 review as the paper-first read-only PM
  review input.
- A fresh post-fix Copilot Opus handoff was not run because the manual-review
  MCP tool is not exposed in this runtime.

## Commit

- Local commit created with message: `Refine ARIS paper cycle 4 framing`.
- Push pending: `git push` failed because HTTPS credentials were unavailable in
  the non-interactive runtime (`could not read Username for 'https://github.com'`).
