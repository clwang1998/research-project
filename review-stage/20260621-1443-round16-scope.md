# round16 Modification Scope

## Review Handoff

- Reviewer: GitHub Copilot Opus
- Executor: Codex
- Prompt file: `tmp/copilot_opus_review_prompt.md`
- Review response file: `review-stage/20260621-1443-round16-copilot-opus.md`
- Scope: `report/sp500_case_study.tex, report/sp500_case_study.pdf`

## Codex Changes In Response

- Updated `report/sp500_case_study.tex` so the paper now distinguishes ensemble
  construction as the prediction output layer from portfolio construction as the
  trading/evaluation layer.
- Added the supervised relation-aware graph variant and validation-selected rank
  ensemble methodology.
- Expanded the portfolio section into a simple-to-strict ladder: long-only top
  bucket, plain long-short decile, sector-neutral long-short decile, beta/sector
  neutral institutional extension, and cost-aware/turnover-controlled variants.
- Added a 2022-2026 sector-neutral 5-day ensemble comparison table sourced from
  `output/model_search/overnight_supervised_gat_sector_neutral_ensemble_metrics.csv`.
- Rebuilt `report/sp500_case_study.pdf`.

## Validation

- `pdflatex -interaction=nonstopmode -halt-on-error sp500_case_study.tex` run
  twice from `report/`; completed successfully.
- Rendered the rebuilt PDF to PNGs with `pdftoppm` and visually checked pages 5,
  6, and 7 for the portfolio ladder, ensemble table, and section transitions.
- Verified key added text with `pdftotext`.
- Verified the ensemble table display values match
  `output/model_search/overnight_supervised_gat_sector_neutral_ensemble_metrics.csv`
  after rounding.

## Remaining Risk

- GitHub Copilot Opus reviewer gate did not run automatically because
  `mcp__manual_review__review` is not exposed in this Codex runtime. The prompt
  was generated at `tmp/copilot_opus_review_prompt.md` and copied to the
  clipboard by `scripts/prepare_copilot_opus_review.py`.
- The paper explicitly states that the fixed ensemble split has COVID-2020
  validation and inflation-2022 test stress rows; 2008 is covered only by the
  broader walk-forward stability analysis, not by the fixed-split ensemble
  table.
