# cycle 3 Scope

- Reviewer: Copilot Claude Opus paper-first quant PM review
- Review file: review-stage/20260621-201250-cycle3-quant-pm-copilot-opus.md
- Executor: Codex cloud
- Log dir: /root/research_project_aris/output/aris_paper_pm_20260621-193610
- Started: 2026-06-21T20:20:21+00:00

## Pending
None.

## Fixes Applied

- Tightened `report/sp500_case_study.tex` around the Route B 30d overlay:
  added the 884 full-grid / 14 same-target DSR trial counts, the expected-max
  null Sharpe of 1.512, and the explicit statement that the overlay is kept for
  a residual sleeve role and lower turnover rather than raw performance
  dominance.
- Clarified the Route B IC convention issue:
  `tab:route_b_30d` now marks overlay ICIR as approximate and the artifact-check
  prose reports the audit-convention delta (`-0.0083` vs `-0.0105`) instead of
  implying an apples-to-apples ICIR gain.
- Strengthened the factor-neutral interpretation:
  the P0 audit table and discussion now explain that neutralization reduces
  annual return from 8.27% to 5.17% and raises turnover from 0.49 to 0.70, so
  the factor-neutral Sharpe is not evidence of clean independent alpha.
- Added a `tab:results` caption note explaining the 20d market-relative block
  versus the 5d/30d sector-relative production benchmark.
- Compressed the Kronos paragraph so it remains only a future point-in-time
  data-universe / survivorship-bias route, not empirical alpha evidence.
- Regenerated tracked `report/sp500_case_study.pdf`.

## Focused Computation

- Plain 30d model deflation was not run. The available Route B audit artifacts
  include per-period prediction/backtest files for the Route B XGBoost/Ridge
  runs, but the plain 30d LightGBM row cited from
  `docs/horizon_comparison.md` is not accompanied by an archived per-period
  prediction/backtest artifact under `output/model_search`; running a DSR/PBO
  audit from only the summarized row would be misleading.

## Validation

- `git diff --check` passed.
- `pdflatex -interaction=nonstopmode -halt-on-error sp500_case_study.tex`
  passed twice from `report/`.
- `python3`/`pypdf` page-count check: `report/sp500_case_study.pdf` is 9 pages.
- LaTeX log check found no overfull-box or unresolved-reference warnings.
- Python compile was not applicable because no Python scripts were touched.

## Git

- Local commit created with message: `Tighten cycle 3 paper audit framing`.
- Push attempted with `git push origin codex/supervised-gat-ensemble-search`
  and failed because HTTPS credentials were unavailable in the unattended
  terminal (`could not read Username for 'https://github.com'`). Push remains
  pending.
