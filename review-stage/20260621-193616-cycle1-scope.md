# cycle 1 Scope

- Reviewer: Copilot Claude Opus paper-first quant PM review
- Review file: review-stage/20260621-193616-cycle1-quant-pm-copilot-opus.md
- Executor: Codex cloud
- Log dir: /root/research_project_aris/output/aris_paper_pm_20260621-193610
- Started: 2026-06-21T19:45:08+00:00

## Codex Changes

- Corrected the main hold-out momentum baseline in `report/sp500_case_study.tex`.
  The 5d sector row now uses the stored canonical artifact
  `output/momentum_baseline_priority/momentum_excess_sector_5d_sector_neutral.json`
  (`Sharpe=0.627`, annual return `5.92%`, turnover `0.221`) instead of the
  unsourced legacy `1.05 / 13.5% / 0.16` figures.
- Ran focused momentum baseline computations for 20d market, 20d sector, and
  30d sector targets using `scripts/eval_momentum_baseline.py`. The new local
  artifacts are:
  - `output/momentum_baseline_priority/momentum_excess_market_20d_sector_neutral.json`
  - `output/momentum_baseline_priority/momentum_excess_sector_20d_sector_neutral.json`
  - `output/momentum_baseline_priority/momentum_excess_sector_30d_sector_neutral.json`
- Replaced the unsourced 20d market momentum row with the computed 20d market
  baseline (`RankIC=0.0418`, `ICIR=0.653`, raw ICIR `3.512`,
  `Sharpe=0.822`, annual return `7.83%`, turnover `0.356`).
- Reconciled Route B 30d overlay metrics to the robustness-audit artifact:
  validation-selected overlay Sharpe is now reported as `0.987`, annual return
  `8.27%`, max drawdown `-7.32%`, and turnover `0.494`.
- Removed the stale Route B convention-mismatch table/claim. The focused 30d
  rerun reproduces the Route B standalone momentum baseline exactly under the
  local evaluator, so the remaining weakness is DSR/PBO and attribution rather
  than artifact provenance.
- Compressed weak graph/GAT/ensemble evidence from tables into prose so the
  report no longer over-promotes those failed directions.
- Updated supporting docs to remove the old `net Sharpe ~1.05` route and the
  stale Route B convention warning:
  - `docs/route_b_residual_alpha_20260622.md`
  - `docs/report_improvement_plan.md`

## Validation

- `python3 scripts/eval_momentum_baseline.py` completed for 20d market, 20d
  sector, and 30d sector baseline checks.
- `git diff --check` passed.
- `python3 -m py_compile scripts/eval_momentum_baseline.py scripts/run_model_pipeline.py`
  passed.
- `python3 -m json.tool` passed for the three new momentum baseline JSON
  artifacts.
- LaTeX compile was not run because no TeX engine (`pdflatex`, `xelatex`,
  `lualatex`, `latexmk`, or `tectonic`) is installed in this runtime.

## Reviewer Gate

- Read-only Copilot Opus cycle review was already present at
  `review-stage/20260621-193616-cycle1-quant-pm-copilot-opus.md`.
- Generated a fresh narrow follow-up review prompt:
  `tmp/copilot_opus_review_prompt.md`.
- The manual-review MCP/browser handoff is not exposed in this Codex runtime,
  and there is no clipboard/browser handoff on the cloud server, so the second
  Copilot Opus pass could not be run unattended. Placeholder files were created
  under `review-stage/20260621-1952-round33-*`.

## Commit / Push

- Local commit created: `Fix ARIS report momentum baseline claims`.
- Push attempted with `git push origin codex/supervised-gat-ensemble-search`.
  It failed because HTTPS credentials are unavailable in the unattended
  terminal: `fatal: could not read Username for 'https://github.com': terminal
  prompts disabled`.
- Push remains pending.

## Remaining Risk

- Page count could not be rechecked without a TeX engine. The report removes
  multiple non-core tables, but final page-count verification remains pending.
- Route B remains exploratory: full-grid DSR is `0.003`, PBO is `50%`, and
  independent style-neutral attribution is weak.
- Survivorship remains bounded by haircut sensitivity, not corrected with
  point-in-time constituents and delisting returns.
